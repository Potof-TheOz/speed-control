const main: SpeedControl.mainFunction = function (
  _turretConfiguration,
  _sensorData
) {
  // code here bros!
  // force et honneur
  return {
    speeding: false,
    speedDelta: 0,
    legalSpeed: 0,
    vehicleLicensePlate: '',
  };
};

export default main;
